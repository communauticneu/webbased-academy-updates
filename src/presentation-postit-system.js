(function(root,factory){
'use strict';
const api=factory(root?.AcademyTextSystem,root);
if(typeof module==='object'&&module.exports)module.exports=api;
if(root)root.AcademyPostItSystem=api;
})(typeof globalThis!=='undefined'?globalThis:this,function(textSystem,root){
'use strict';
const POSTIT_PALETTE=Object.freeze({darkYellow:'#c99a00',green:'#4f9b57',beige:'#c9b88a',red:'#a83d36',cyan:'#4f9fb4',lightGray:'#b8b8b2'});
const DEFAULT_POSTIT=Object.freeze({content:'Neues Post it',colorKey:'darkYellow',align:'center',width:330,height:58,minWidth:330,minHeight:58,x:64,y:72});
const POSTIT_MEDIUM_PROFILES=Object.freeze({none:Object.freeze({fontFamily:'Trebuchet MS',fontWeight:700,fontSize:39,textColor:'#202020',surface:'paper'}),board:Object.freeze({fontFamily:'KG Second Chances Sketch',fontWeight:400,fontSize:39,textColor:'#ffffff',surface:'chalkboard'})});
let nextId=1;
function number(value,fallback){return Number.isFinite(value)?value:fallback;}
function createPostIt(overrides={}){const colorKey=Object.prototype.hasOwnProperty.call(POSTIT_PALETTE,overrides.colorKey)?overrides.colorKey:DEFAULT_POSTIT.colorKey;const align=['left','center','right'].includes(overrides.align)?overrides.align:DEFAULT_POSTIT.align;const minWidth=Math.max(DEFAULT_POSTIT.minWidth,number(overrides.minWidth,DEFAULT_POSTIT.minWidth)),minHeight=Math.max(DEFAULT_POSTIT.minHeight,number(overrides.minHeight,DEFAULT_POSTIT.minHeight));return {id:typeof overrides.id==='string'&&overrides.id?overrides.id:`academy-postit-${nextId++}`,content:typeof overrides.content==='string'?overrides.content:DEFAULT_POSTIT.content,colorKey,align,width:Math.max(minWidth,number(overrides.width,DEFAULT_POSTIT.width)),height:Math.max(minHeight,number(overrides.height,DEFAULT_POSTIT.height)),minWidth,minHeight,x:Math.max(0,number(overrides.x,DEFAULT_POSTIT.x)),y:Math.max(0,number(overrides.y,DEFAULT_POSTIT.y))};}
function resolvePostItStyle(item,medium='none'){if(!item)throw new Error('Post-it object required');const own=POSTIT_MEDIUM_PROFILES[medium]||POSTIT_MEDIUM_PROFILES.none;if(medium==='board'&&textSystem?.MEDIUM_PROFILES?.board){const board=textSystem.MEDIUM_PROFILES.board;return {fontFamily:board.fonts.heading,fontWeight:board.weights.heading,fontSize:board.sizes.heading,textColor:board.defaultColor,paperColor:POSTIT_PALETTE[item.colorKey],surface:own.surface};}return {...own,paperColor:POSTIT_PALETTE[item.colorKey]};}
function duplicatePostIt(item){return createPostIt({...item,id:undefined,x:item.x+18,y:item.y+18});}
function createEngine(){const objects=[];let selectedId=null,editingId=null,medium='none';const find=id=>objects.find(item=>item.id===id)||null;const copy=item=>item?{...item}:null;
 function addPostIt(overrides={}){const item=createPostIt(overrides);objects.push(item);selectedId=item.id;editingId=null;return copy(item);}
 function select(id){if(id===null){selectedId=null;editingId=null;return true;}if(!find(id))return false;selectedId=id;if(editingId!==id)editingId=null;return true;}
 function beginEdit(id=selectedId){if(!id||!find(id))return false;selectedId=id;editingId=id;return true;}
 function endEdit(){if(!editingId)return false;editingId=null;return true;}
 function updateContent(content){const item=find(editingId);if(!item||typeof content!=='string')return false;item.content=content;return true;}
 function moveSelected(dx,dy){const item=find(selectedId);if(!item||editingId)return false;item.x=Math.max(0,item.x+number(dx,0));item.y=Math.max(0,item.y+number(dy,0));return true;}
 function resizeSelected(width,height){const item=find(selectedId);if(!item||editingId)return false;item.width=Math.max(DEFAULT_POSTIT.minWidth,number(width,item.width));item.height=Math.max(DEFAULT_POSTIT.minHeight,number(height,item.height));item.minWidth=item.width;item.minHeight=item.height;return true;}
 function autoGrowSelected(width,height){const item=find(selectedId);if(!item)return false;const nextWidth=Math.max(item.width,item.minWidth,number(width,item.width)),nextHeight=Math.max(item.height,item.minHeight,number(height,item.height));if(nextWidth===item.width&&nextHeight===item.height)return false;item.width=nextWidth;item.height=nextHeight;return true;}
 function setAlignment(align){const item=find(selectedId);if(!item||!['left','center','right'].includes(align))return false;item.align=align;return true;}
 function setColor(colorKey){const item=find(selectedId);if(!item||!Object.prototype.hasOwnProperty.call(POSTIT_PALETTE,colorKey))return false;item.colorKey=colorKey;return true;}
 function duplicateSelected(){const item=find(selectedId);if(!item)return null;const duplicate=duplicatePostIt(item);objects.push(duplicate);selectedId=duplicate.id;editingId=null;return copy(duplicate);}
 function deleteSelected(){if(!selectedId||editingId)return false;const index=objects.findIndex(item=>item.id===selectedId);if(index<0)return false;objects.splice(index,1);selectedId=null;return true;}
 function setMedium(next){medium=POSTIT_MEDIUM_PROFILES[next]?next:'none';return true;}
 return Object.freeze({addPostIt,select,beginEdit,endEdit,updateContent,moveSelected,resizeSelected,autoGrowSelected,setAlignment,setColor,duplicateSelected,deleteSelected,setMedium,getObject:id=>copy(find(id)),getObjects:()=>objects.map(copy),getState:()=>({selectedId,editingId,medium}),getResolvedStyle:id=>{const item=find(id);return item?resolvePostItStyle(item,medium):null;}});}
let browserEngine=null,browserDoc=null,academyPostItLayer=null,drag=null;
function ensureStyle(doc){if(doc.getElementById('academyPostItStyle'))return;const style=doc.createElement('style');style.id='academyPostItStyle';style.textContent=`#academyPostItLayer{position:absolute;inset:0;z-index:6;pointer-events:none;overflow:hidden}.academy-postit-paper{position:absolute;box-sizing:border-box;display:flex;align-items:center;padding:7px 22px 9px;pointer-events:auto;cursor:grab;user-select:none;background:linear-gradient(103deg,rgba(255,255,255,.22) 0%,rgba(255,255,255,.04) 28%,rgba(0,0,0,.05) 100%),var(--postit-paper);box-shadow:3px 7px 12px rgba(0,0,0,.34),inset 0 1px rgba(255,255,255,.18);transform:rotate(-.18deg);filter:saturate(.96)}.academy-postit-paper::before{content:'';position:absolute;inset:0;pointer-events:none;background:linear-gradient(90deg,rgba(255,255,255,.13),transparent 32%,rgba(0,0,0,.045));mix-blend-mode:soft-light}.academy-postit-paper.is-selected{outline:1px solid rgba(106,211,255,.88);outline-offset:2px}.academy-postit-text{position:relative;z-index:2;width:100%;outline:0;white-space:pre-wrap;line-height:1.05;cursor:inherit}.academy-postit-paper.is-editing .academy-postit-text{cursor:text;user-select:text}.academy-postit-fold{position:absolute;right:-1px;bottom:-1px;width:25px;height:18px;pointer-events:none;background:linear-gradient(145deg,rgba(255,255,255,.35),rgba(0,0,0,.11));clip-path:polygon(100% 0,100% 100%,0 100%);filter:drop-shadow(-3px -2px 2px rgba(0,0,0,.16))}`;doc.head?.appendChild(style);}
function findStage(doc){return doc.getElementById('academyStage')||doc.querySelector('.academy-stage')||doc.getElementById('stage')||doc.querySelector('.stage');}
function growFromText(text){if(!text||!browserEngine)return;const paper=text.closest?.('.academy-postit-paper');if(!paper)return;const safeWidth=Math.ceil(text.scrollWidth+46),safeHeight=Math.ceil(text.scrollHeight+18);if(browserEngine.autoGrowSelected(safeWidth,safeHeight))render();}
function render(){if(!browserDoc||!academyPostItLayer||!browserEngine)return;const state=browserEngine.getState();academyPostItLayer.replaceChildren();browserEngine.getObjects().forEach(item=>{const resolved=browserEngine.getResolvedStyle(item.id);const paper=browserDoc.createElement('div');paper.className='academy-postit-paper'+(state.selectedId===item.id?' is-selected':'')+(state.editingId===item.id?' is-editing':'');paper.dataset.postitId=item.id;paper.style.left=item.x+'px';paper.style.top=item.y+'px';paper.style.width=item.width+'px';paper.style.height=item.height+'px';paper.style.setProperty('--postit-paper',resolved.paperColor);const text=browserDoc.createElement('div');text.className='academy-postit-text';text.textContent=item.content;text.contentEditable=state.editingId===item.id?'true':'false';text.spellcheck=false;text.style.fontFamily=resolved.fontFamily;text.style.fontWeight=String(resolved.fontWeight);text.style.fontSize=resolved.fontSize+'px';text.style.color=resolved.textColor;text.style.textAlign=item.align;const fold=browserDoc.createElement('span');fold.className='academy-postit-fold';paper.append(text,fold);academyPostItLayer.appendChild(paper);if(state.editingId===item.id){requestAnimationFrame?.(()=>{text.focus();});}});}
function install(doc){if(!doc)return false;browserDoc=doc;ensureStyle(doc);const stage=findStage(doc);if(!stage)return false;if(!academyPostItLayer){academyPostItLayer=doc.createElement('div');academyPostItLayer.id='academyPostItLayer';stage.appendChild(academyPostItLayer);}browserEngine=browserEngine||createEngine();if(!academyPostItLayer.dataset.bound){academyPostItLayer.dataset.bound='true';academyPostItLayer.addEventListener('click',e=>{const paper=e.target.closest?.('.academy-postit-paper');if(!paper)return;browserEngine.select(paper.dataset.postitId);render();});academyPostItLayer.addEventListener('dblclick',e=>{const paper=e.target.closest?.('.academy-postit-paper');if(!paper)return;browserEngine.beginEdit(paper.dataset.postitId);render();});academyPostItLayer.addEventListener('input',e=>{if(!e.target.classList?.contains('academy-postit-text'))return;browserEngine.updateContent(e.target.textContent||'');growFromText(e.target);});academyPostItLayer.addEventListener('focusout',e=>{if(!e.target.classList?.contains('academy-postit-text'))return;browserEngine.updateContent(e.target.textContent||'');browserEngine.endEdit();render();});academyPostItLayer.addEventListener('pointerdown',e=>{const paper=e.target.closest?.('.academy-postit-paper');if(!paper||browserEngine.getState().editingId)return;browserEngine.select(paper.dataset.postitId);drag={x:e.clientX,y:e.clientY};paper.setPointerCapture?.(e.pointerId);render();});doc.addEventListener('pointermove',e=>{if(!drag)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;drag={x:e.clientX,y:e.clientY};browserEngine.moveSelected(dx,dy);render();});doc.addEventListener('pointerup',()=>{drag=null;});doc.addEventListener('keydown',e=>{if(e.key!=='Delete'||browserEngine.getState().editingId)return;if(browserEngine.deleteSelected()){e.preventDefault();render();}});}
 if(!doc.documentElement.dataset.academyPostItMediumBound){doc.documentElement.dataset.academyPostItMediumBound='true';root?.addEventListener?.('academy-presentation-medium-change',e=>{browserEngine.setMedium(e.detail?.medium||'none');render();});}render();return true;}
function addPostIt(overrides={}){if(!browserEngine&&browserDoc)install(browserDoc);if(!browserEngine)return null;const item=browserEngine.addPostIt(overrides);render();return item;}
if(root?.document){const boot=()=>install(root.document)||setTimeout(()=>install(root.document),60);if(root.document.readyState==='loading')root.document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();}
return Object.freeze({POSTIT_PALETTE,DEFAULT_POSTIT,POSTIT_MEDIUM_PROFILES,createPostIt,resolvePostItStyle,duplicatePostIt,createEngine,install,addPostIt});
});
